# Factories

These can be imported at the web interface:

Factories > Create Factory > Config > Upload File

Each factory sets up a workspace and creates one or more projects in that workspace.


## c.json

A single machine workspace with cmake installed
